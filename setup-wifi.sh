
# separate script to setup wifi (for laptops, ...)
apt install -y network-manager

# might be required
#apt install -y firmware-iwlwifi

#nmcli dev wifi list
#nmcli dev wifi connect $network-ssid password $password
